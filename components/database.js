import {useState, useEffect} from '/react'
export default function database({form}) {
const [user, setUser]=useState=''
form=new FormData.get((data)=>{
    data&&data.filter((item)=> item)
})


  return (
    <div>
      
    </div>
  )
}
